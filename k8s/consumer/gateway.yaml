apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: consumer-gateway
  namespace: consumer
spec:
  gatewayClassName: traefik
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: All