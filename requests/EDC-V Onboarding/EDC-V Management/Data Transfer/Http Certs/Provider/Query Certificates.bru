meta {
  name: Query Certificates
  type: http
  seq: 2
}

post {
  url: {{dpBaseUrl}}/app/internal/api/control/certs/request
  body: json
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
