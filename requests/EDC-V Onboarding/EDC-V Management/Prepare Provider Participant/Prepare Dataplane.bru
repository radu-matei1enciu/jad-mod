meta {
  name: Prepare Dataplane
  type: http
  seq: 4
}

post {
  url: {{cpBaseUrl}}/api/mgmt/v5beta/dataplanes/{{provider_id}}
  body: json
  auth: inherit
}

body:json {
  {
    "id": "provider-siglet",
    "allowedTransferTypes": [
      "HttpData-PULL"
    ],
    "url": "http://siglet.edc-v.svc.cluster.local:8081/api/v1/{{provider_id}}/dataflows"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
