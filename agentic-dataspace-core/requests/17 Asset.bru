meta {
  name: 17 Asset
  type: http
  seq: 17
}

post {
  url: {{cpBaseUrl}}/v5beta/participants/{{provider_context_id}}/assets
  body: json
  auth: inherit
}

body:json {
  {
    "@context": [
      "https://w3id.org/edc/connector/management/v2"
    ],
    "@type": "Asset",
    "@id": "{{ASSET_CERT_ID}}",
    "properties": {
      "description": "This asset requires the Membership credential to access"
    },
    "privateProperties": {
      "permission": "{{ASSET_PERMISSION}}"
    }
  }
}

vars:pre-request {
}

settings {
  encodeUrl: true
  timeout: 0
}
