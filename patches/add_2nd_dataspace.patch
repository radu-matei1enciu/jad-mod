diff --git a/k8s/apps/redline-seed-job.yaml b/k8s/apps/redline-seed-job.yaml
index 854bfa9..7742f68 100644
--- a/k8s/apps/redline-seed-job.yaml
+++ b/k8s/apps/redline-seed-job.yaml
@@ -87,6 +87,19 @@ spec:
               DATASPACE_ID=$(echo "$DATASPACE_RESPONSE" | jq -r '.id')
               echo "Dataspace $DATASPACE_ID created successfully"
               
+              # Create a second Dataspace: Bike-X
+              echo "Creating Bike-X Dataspace..."
+              BIKEX_RESPONSE=$(curl -sf -X POST "$REDLINE_BASE_URL/dataspaces" \
+                  -H "content-type: application/json" \
+                  -d '{
+                    "name": "Bike-X",
+                    "properties": {
+                      "description": "A Mock Bicycle-Industry Dataspace"
+                    }
+                  }')
+              BIKEX_ID=$(echo "$BIKEX_RESPONSE" | jq -r '.id')
+              echo "Bike-X Dataspace $BIKEX_ID created successfully"
+
               # Create Tenant
               echo "Seed Consumer Tenant"
               TENANT_RESPONSE=$(curl -sf -X POST "$REDLINE_BASE_URL/service-providers/$SP_ID/tenants" \
